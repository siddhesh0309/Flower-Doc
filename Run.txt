from fastapi import FastAPI
from app.controllers.pan_controller import router as pan_router

app = FastAPI(title="RBI LRS Gateway", version="1.0.0")
app.include_router(pan_router, prefix="/lrs", tags=["LRS"])

@app.get("/healthz")
def healthz():
    return {"ok": True}

if __name__ == "__main__":
    import uvicorn
    uvicorn.run("run:app", host="0.0.0.0", port=8000, reload=True)
